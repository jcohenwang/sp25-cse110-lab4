1. values added: 20
2. values added: 20
3. var is either function scoped or global scoped which means it is often exists outside of where it is declared, it also raised to the top of the function/entire program which is weird and unintuitive. If you are using var in different blocks you have to use different names or theyll refer to the same instance.
4. values added: 20
5. error, because result was not declared outside of the if block and it is called outside of its scope in line 13
6. values added: 20
7. error, because result was not declared outside of the if block and it is called outside of its scope in line 13